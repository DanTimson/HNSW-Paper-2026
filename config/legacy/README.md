Pre-BIGANN C++ sweep configs (ANN_SIFT1M, GIST1M). These produced the
`results/legacy/*.jsonl` logs behind the paper's early SIFT1M/GIST1M sections.
Superseded by the BIGANN scale-sweep configs (`config/bigann*_sweep.json`).
A future GIST scale sweep should follow the bigann template, not these.
